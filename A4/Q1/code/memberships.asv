function [ u ] = memberships( w,y,c,b,mask,u,K )
%memberships Finds optimal values of memberships within every iteration

imgSize = size(y);
% u = zeros(imgSize);
d = zeros(K,1);

for i=1:imgSize(1)
   for j=1:imgSize(2)
      if(mask(i,j)>0)
          for k = 1:1:K     
            for l = -windowRadius:1:windowRadius
                for m =  -windowRadius:1:windowRadius
      end
   end
end

end

